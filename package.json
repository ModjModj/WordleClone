{
  "name": "wordle",
  "version": "0.1.0",
  "private": true,
  "packageManager": "yarn@3.2.4",
  "scripts": {
    "build": "yarn build"
    "netlify:deploy": "netlify deploy --auth $NETLIFY_AUTH_TOKEN"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "netlify-cli": "^2.8.3"
  }
}
